.DS_Store

# Claude Design export leftovers: ~87MB of reference screenshots of the live
# site, plus pasted images from the build session. Nothing in any page or
# stylesheet references them, and they would be ~90% of the repo by size.
# They stay on Bob's disk but are NOT pushed.
# NOTE: that means they are not backed up anywhere. They are re-creatable
# (screenshots of impactplus.com), so this is an accepted trade, not an oversight.
impactplus/design-system/scraps/
impactplus/design-system/uploads/
