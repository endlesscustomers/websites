One-sentence: A numbered process-step tab (colored header + label) used in the "Path to Endless Customers" band.

```jsx
<StepCard number="01" title="Learn the System" color="var(--ec-blue-600)"
  caption="Read the book and get aligned." />
```

Vary `color` across steps using the brand/service palette (blue, green, AI teal…).
