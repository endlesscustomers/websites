One-sentence: Inline email-capture form (pill field + blue submit) for the newsletter and free-chapter CTAs.

```jsx
<NewsletterForm buttonLabel="Get My Copy" onDark onSubmit={(email)=>…} />
```

Use `onDark` on the black book-CTA band, `layout="stacked"` in narrow columns.
