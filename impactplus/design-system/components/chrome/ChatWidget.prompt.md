One-sentence: The fixed "Ecee" AI chat launcher — purple→pink gradient bubble with a speech tooltip, bottom-right on every page.

```jsx
<ChatWidget />
```

`position: fixed` — mount once per page; toggling the bubble shows/hides the tooltip.
