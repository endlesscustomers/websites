One-sentence: IMPACT's full site footer — link columns, brand lockup + address, social icons, and the legal line.

```jsx
<Footer />                 // ships with IMPACT's real column structure
<Footer columns={myColumns} address={"123 Main St\nCity, ST"} />
```

Social icons are placeholder letter-chips; swap for IMPACT's brand social SVGs when localizing.
